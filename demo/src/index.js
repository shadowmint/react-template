import {Template} from '../../src/template.jsx';
import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <div>
    <Template>
      Hello world
    </Template>
  </div>, document.getElementById('root')
);